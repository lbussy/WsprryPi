# Issue 411 Slice 10: wspr5 source resynchronization result

## Result

Passed on 2026-08-17. The primary WsprryPi checkout on `wspr5` was safely
fast-forwarded from `00f093c8523d2068740d0371526d2340d8d99379` to
`6eadbc17d2771d885125f9a5e41029fffed3ffec`, the exact current `origin/devel`
tip. The normal non-hardware validation passed on the Pi.

This result qualifies only source-checkout synchronization, monorepo component
shape, compilation, and software regression behavior. It does not qualify an
installed binary, service operation, physical I2C or GPIO behavior, Si5351
output, timing, frequency accuracy, or RF.

## Guarded fast-forward

Before fetching, `/home/pi/WsprryPi` was confirmed to be:

- on branch `devel`;
- clean;
- at the expected pre-integration commit `00f093c`; and
- service-inactive with no open I2C, gpiochip, or RP1 device handle.

After `git fetch origin --prune`, `origin/devel` resolved to the expected
`6eadbc1` tip. The old checkout tip was proven to be the merge base, and
`git merge --ff-only origin/devel` completed without conflict. No merge commit,
rebase, reset, clean, stash, or history rewrite was used.

## Component verification

`.gitmodules` is absent. Each former submodule root exists as a Git tree with
ordinary parent-repository files, and none contains a nested `.git` entry:

- `WsprryPi-UI`;
- `src/INI-Handler`;
- `src/LCBLog`;
- `src/Mailbox`;
- `src/MonitorFile`;
- `src/PPM-Manager`;
- `src/Signal-Handler`;
- `src/Singleton`;
- `src/WSPR-Transmitter`; and
- `src/WSPR-Reference`.

No secondary or qualification worktree was changed.

## Non-hardware validation

The `semantics-test` recipe was inspected before execution. From `src`, the
following command passed:

```sh
WSPRRYPI_DISABLE_HARDWARE_ACCESS=1 make -j2 semantics-test SUDO=
```

Passing coverage included:

- runtime semantics;
- cleanup lifecycle;
- UI/source regression;
- GPIO band fail-closed policy;
- log timestamp display; and
- update-check comparison.

Messages describing transmissions and test tones were generated by the test
fixtures while hardware access was disabled. They were not live transmitter
operations. No device node was opened and no RF was generated.

The run-created `/home/pi/WsprryPi/src/build` directory was removed afterward.
The broader `make clean` target was deliberately not used because it would also
remove the separate `executables` directory.

## Final preservation state

The primary checkout finished clean on `devel`, with both `HEAD` and
`origin/devel` at `6eadbc17d2771d885125f9a5e41029fffed3ffec`. The
`wsprrypi.service` remained inactive, no I2C/gpiochip/RP1 device handle was
open, and `src/build` was absent.

No production application was installed or run, no service or system
configuration was changed, and no physical hardware or RF path was exercised.
Issue 411 remained open and otherwise unchanged.
